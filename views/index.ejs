<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- <link rel="stylesheet" href="/public/stylesheets/style.css"> -->
    

</head>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 ">
        <div class="form p-10 text-white">  
            <form action="/create" method="POST">
                <input class="block w-full rounded-lg outline-none px-5 py-2 bg-zinc-800" type="text" placeholder="Title of the Note" name="title">
                <textarea class="block w-full rounded-lg outline-none px-5 py-2 mt-2 bg-zinc-800 resize-none" placeholder="Write your note" name="details"></textarea>
                <input class="bg-blue-600 mt-2 rounded-lg px-3 py-1 cursor-pointer" type="submit" value="Create Task">
            </form>
        </div>

        <div class="tasks px-10 flex flex-wrap gap-2">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-4 py-3 rounded bg-zinc-800">
                        <h1 class="text-white text-xl tracking-tight"><%=val%></h1>

                        <div class="flex justify-between mt-3">
                        <a class="text-blue-500 inline-block" href="/file/<%=val%>">Open File </a>
                        <a href="/edit/<%=val%>" class="text-zinc-500">Edit File Name</a>
                        </div>
                        

                    </div>
                <% }) %>

            <% } else{ %>
                <h3 class="text-white">No Task Here</h3>
            <% } %>

        </div>
        
        
    </div>
    
<script src="/public/javascripts/script.js "></script>
</body>
</html>